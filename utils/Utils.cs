using System;

namespace bank_application.utils
{
    // This class holds reusable utils, enums, etc.
    public static partial class Utils {
        // Enum for Transaction Type
        public enum TransactionType { INTEREST_EARNED, DEPOSIT, WITHDRAW };
    }
}